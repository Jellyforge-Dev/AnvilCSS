# Serves the built userscript at http://<host>:8283/anvil-customizer.user.js for Tampermonkey
# to install directly — nothing else runs in this container.
services:
  anvilcss:
    build: .
    container_name: anvilcss
    ports:
      - "8283:80"
    restart: unless-stopped
